{
  "name": "@viralscopes/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-plugin-import": "^2"
  }
}
