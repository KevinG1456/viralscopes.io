{
  "name": "viralscopes",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "secretlint": "secretlint --secretlintrc .secretlintrc.json \"**/*\"",
    "prepare": "husky",
    "db:migrate": "npm run db:migrate --workspace=packages/db",
    "db:migrate:down": "npm run db:migrate:down --workspace=packages/db",
    "db:migrate:status": "npm run db:migrate:status --workspace=packages/db",
    "db:setup-roles": "npm run db:setup-roles --workspace=packages/db",
    "db:seed": "npm run db:seed --workspace=packages/db",
    "db:reset": "npm run db:reset --workspace=packages/db",
    "db:studio": "npm run db:studio --workspace=packages/db",
    "workflows:import": "node infra/n8n-workflows/sync.mjs import",
    "workflows:export": "node infra/n8n-workflows/sync.mjs export",
    "ai:regression": "npm run ai:regression --workspace=apps/api"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "@secretlint/secretlint-rule-preset-recommend": "^13",
    "eslint": "^10",
    "eslint-plugin-import": "^2",
    "husky": "^9",
    "lint-staged": "^17",
    "prettier": "^3",
    "secretlint": "^13",
    "turbo": "^2",
    "typescript": "^7",
    "typescript-eslint": "^8"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "npm@11.13.0",
  "overrides": {
    "nanoid": "^3.3.18"
  }
}
